using System;

namespace Word2_Task3
{
    class Program
    {
        static void Main(string[] args)
        {
            int x = 13;
            int y = 4;
            int z = 37;
            int avg = (x + y + z) / 3;
            Console.WriteLine($"Better is to use double but in task i must write in int type so Avg Int = {avg}");
        }
    }
}
