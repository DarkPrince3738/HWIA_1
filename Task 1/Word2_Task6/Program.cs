using System;

namespace Word2_Task6
{
    class Program
    {
        static void Main(string[] args)
        {
            //int uberflu ? (we can't)
            //int _Identifier (we can't)
            //int \ u006fIdentifier (we can't)
            //int &myVar (we can't)
            int myVariab1le = 5;
        }
    }
}
